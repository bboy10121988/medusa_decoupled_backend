# CORS 設定（使用空格分隔而非逗號）
STORE_CORS=https://timsfantasyworld.com https://admin.timsfantasyworld.com
ADMIN_CORS=https://timsfantasyworld.com https://admin.timsfantasyworld.com  
AUTH_CORS=https://timsfantasyworld.com https://admin.timsfantasyworld.com
EOF && echo "應用CORS更新:" && cat .env.cors.update >> .env.temp && grep -v -E "^(STORE_CORS|ADMIN_CORS|AUTH_CORS)=" .env >> .env.temp && mv .env.temp .env && echo "確認更新:" && grep -E "(STORE_CORS|ADMIN_CORS|AUTH_CORS)" .env
